// 配置
const config = {
  output: './',
  origin: 'https://muc.cheshirex.com',
  adapter: 'default',
  downloadLRC: false,
  verbose: false
}

// 读取全局配置文件
function loadGlobalConfig() {}

// 根据命令行参数返回当前命令行运行时的配置
function getCliConfig(args) {}

module.exports = config
